// src/bubble/types.ts — Re-export of the canonical permission
// types from the shared location. The bubble is one of two
// consumers (bubble + devtools); the source of truth is
// `src/types/permission.ts`.

export * from "../types/permission";
